- name: get firefox profile folder path
  shell: ls /home/sjas/.mozilla/firefox/*default -d
  register: ff_path
  changed_when: false

- name: deploy firefox settings
  become: yes
  become_user: sjas
  copy:
    src: user.js
    dest: "{{ ff_path.stdout }}"